---
import Logo from "../components/Logo.astro";
import Link from "./Link.astro";

export interface Props {
    title?: string;
}

interface Link {
    title: string;
    target?: string;
    href: string;
}

const { title } = Astro.props;

const links: Link[] = [
    {
        title: "projects",
        href: "/projects",
    },
    {
        target: "_blank",
        title: "resume",
        href: "https://docs.google.com/document/d/1k8UKC6QRw4NxkY-lMdF1ShyOLkb9w2ugVLDQf63AKNo/edit?usp=sharing",
    },
];
---

<header class="flex justify-between py-6 px-16">
    <a href="/">
        <Logo />
    </a>
    <div class="flex gap-6 items-center">
        {
            links.map((link) => {
                if (link.title === title) {
                    return (
                        <div>
                            <span>[X]</span>
                            <span>{title}</span>
                        </div>
                    );
                }
                return (
                    <Link
                        href={link.href}
                        title={link.title}
                        target={link.target}
                    />
                );
            })
        }
    </div>
</header>
